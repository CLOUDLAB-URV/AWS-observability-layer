node_modules/
dist/
server/data/
server/persistence/

# Env files: commit non-secret flag files (.env, .env.development, .env.production);
# keep secrets and machine-specific overrides out of git.
.env.local
.env.*.local

# VPS deployment config: keep the real files local, commit the .example templates.
# (.env = stack config: DOMAIN/images/modes; .env.deploy = push-deploy SSH target)
deploy/vps/.env
deploy/vps/.env.deploy
